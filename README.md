# react-components-libraryimport { Button } from 'react-components-library';

function MyComponent() {
  return (
    <Button onClick={() => console.log('Button clicked!')}>
      Click me!
    </Button>
  );
}
